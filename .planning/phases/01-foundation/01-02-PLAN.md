---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - Sources/Core/DataDependency/DIContainer.swift
  - Sources/Data/DataSource/LocalDataSource.swift
  - Sources/Data/Repositories/UserRepository.swift
  - Sources/Domain/Entities/user.swift
autonomous: true

must_haves:
  truths:
    - "SwiftData ModelContainer configured"
    - "DIContainer provides all dependencies"
    - "LocalDataSource wraps SwiftData operations"
    - "UserRepository protocol + implementation exist"
  artifacts:
    - path: "Sources/Core/DataDependency/DIContainer.swift"
      provides: "Dependency injection container"
      min_lines: 30
      exports: ["DIContainer"]
    - path: "Sources/Data/DataSource/LocalDataSource.swift"
      provides: "SwiftData operations wrapper"
      min_lines: 25
    - path: "Sources/Data/Repositories/UserRepository.swift"
      provides: "User data access protocol+impl"
      min_lines: 20
    - path: "Sources/Domain/Entities/user.swift"
      provides: "User SwiftData model"
      contains: "@Model"
  key_links:
    - from: "DIContainer"
      to: "ModelContainer"
      via: "lazy var modelContainer"
    - from: "DIContainer"
      to: "LocalDataSource"
      via: "lazy var localDataSource"
    - from: "LocalDataSource"
      to: "SwiftData"
      via: "ModelContext"
---

<objective>
Implement data layer foundation: SwiftData, DI container, repositories.

Purpose: Provide data persistence and dependency injection for all features. User entity and repository establish patterns for other entities.

Output: Working DIContainer, SwiftData setup, UserRepository ready for auth feature.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/PROJECT_RULES.md
@.planning/REQUIREMENTS.md

Requirements this plan satisfies:
- DATA-01: SwiftData Setup
- DATA-04: Repository Pattern (UserRepository first)
- AUTH-02: User Record Persistence (User entity)

User entity fields from AUTH-02:
- id, authProvider, email, name, createdAt
- hasCompletedOnboarding, isPremium, subscriptionExpiryDate
- streak tracking: currentStreak, longestStreak, lastEngagementDate

Follows PROJECT_RULES.md patterns for:
- Entity naming (PascalCase class, snake_case file)
- Repository protocol + implementation
- DIContainer lazy var dependencies
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create User entity and SwiftData setup</name>
  <files>Sources/Domain/Entities/user.swift, Sources/Core/DataDependency/DIContainer.swift</files>
  <action>
    user.swift:
    - @Model class User: identifiable
    - Fields per AUTH-02 requirement (all optional except id/createdAt)
    - File name: snake_case (user.swift)
    - init method with required fields

    DIContainer.swift:
    - static let shared singleton
    - private init with ModelContainer
    - lazy var modelContainer: ModelContainer (for: User.self)
    - Handle ModelContainer creation failure with inMemory fallback
    - All dependencies as lazy var (not implemented yet, stub with TODO)

    Follow PROJECT_RULES.md DIContainer pattern exactly
  </action>
  <verify>
    - xcodebuild builds successfully
    - User has @Model decorator
    - DIContainer.shared exists
  </verify>
  <done>User entity defined, SwiftData container in DIContainer</done>
</task>

<task type="auto">
  <name>Task 2: Implement LocalDataSource</name>
  <files>Sources/Data/DataSource/LocalDataSource.swift</files>
  <action>
    LocalDataSource.swift per PROJECT_RULES.md:
    - final class LocalDataSource
    - let container: ModelContainer, let context: ModelContext
    - init(container: ModelContainer) stores container, creates context
    - func getUser() throws -> User? (using FetchDescriptor)
    - func saveUser(_ user: User) throws (context.insert + context.save)
    - func updateUser(_ apply: (User) -> Void) throws

    Throw errors, don't catch (caller handles)
    Use SwiftData FetchDescriptor for queries
    Manual context.save() after mutations
  </action>
  <verify>
    - LocalDataSource compiles
    - Has getUser, saveUser, updateUser methods
    - Uses FetchDescriptor for queries
  </verify>
  <done>LocalDataSource wraps SwiftData operations</done>
</task>

<task type="auto">
  <name>Task 3: Implement UserRepository protocol and impl</name>
  <files>Sources/Data/Repositories/UserRepository.swift</files>
  <action>
    UserRepository.swift per PROJECT_RULES.md:
    - protocol UserRepository (getUser, createUser, updateUser methods)
    - class UserRepositoryImpl: UserRepository
    - init(localDataSource: LocalDataSource)
    - Delegate to LocalDataSource methods
    - Use async throws for async operations
    - Return types explicit

    Add to DIContainer:
    - lazy var localDataSource: LocalDataSource
    - lazy var userRepository: UserRepository = UserRepositoryImpl(localDataSource: localDataSource)

    Protocol-first, inject LocalDataSource, async throws pattern
  </action>
  <verify>
    - UserRepository protocol defines methods
    - UserRepositoryImpl implements protocol
    - DIContainer has userRepository lazy var
    - xcodebuild succeeds
  </verify>
  <done>UserRepository ready for auth feature, DI complete</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] xcodebuild succeeds
- [ ] User has @Model decorator
- [ ] DIContainer.shared singleton exists
- [ ] LocalDataSource wraps SwiftData with ModelContext
- [ ] UserRepository protocol + impl exist
- [ ] All dependencies injected via DIContainer
</verification>

<success_criteria>
- SwiftData ModelContainer configured with User entity
- DIContainer provides LocalDataSource and UserRepository
- Repository pattern established for other entities to follow
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
