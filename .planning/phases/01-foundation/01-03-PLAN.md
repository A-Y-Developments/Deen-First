---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on: ["02"]
files_modified:
  - Sources/Core/Networking/HTTPClient.swift
  - Sources/Presentation/Components/CustomButton.swift
  - Sources/Domain/Entities/user.swift (relationships)
autonomous: true

must_haves:
  truths:
    - "HTTPClient can make GET requests with retry"
    - "CustomButton component displays with variants"
    - "User entity has relationships to Session/BlockedApp/TimeLimit"
  artifacts:
    - path: "Sources/Core/Networking/HTTPClient.swift"
      provides: "Generic network client"
      min_lines: 40
      exports: ["HTTPClient"]
    - path: "Sources/Presentation/Components/CustomButton.swift"
      provides: "Reusable button component"
      min_lines: 25
      exports: ["CustomButton"]
    - path: "Sources/Domain/Entities/user.swift"
      provides: "User with SwiftData relationships"
      contains: "@Relationship"
  key_links:
    - from: "HTTPClient"
      to: "URLSession"
      via: "shared session"
    - from: "User"
      to: "Session"
      via: "@Relationship"
    - from: "User"
      to: "BlockedApp"
      via: "@Relationship"
---

<objective>
Implement HTTP client for API calls, reusable UI component, and entity relationships.

Purpose: Complete foundation infrastructure - networking for Quran API, UI component for onboarding, data relationships for features.

Output: HTTPClient with retry logic, CustomButton component, User with relationships.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/PROJECT_RULES.md
@.planning/REQUIREMENTS.md

Requirements this plan satisfies:
- DATA-02: HTTP Client (retry, timeout, error handling)
- UI-01: Custom Button (variants, loading, disabled)
- AUTH-02: User relationships (sessions, blockedApps, appTimeLimits)

HTTPClient spec from DATA-02:
- Generic GET support
- 3 retry attempts with exponential backoff
- 30 second timeout
- Error handling

CustomButton spec from UI-01:
- Title and action closure
- Loading state
- Disabled state
- Dense and regular variants
- Located in Presentation/Components/

Entity relationships from AUTH-02:
- User has relationships to: sessions, blockedApps, appTimeLimits
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement HTTPClient with retry logic</name>
  <files>Sources/Core/Networking/HTTPClient.swift</files>
  <action>
    HTTPClient.swift:
    - final class HTTPClient (singleton: static let shared)
    - func get<T: Decodable>(url: URL) async throws -> T
    - Retry logic: 3 attempts with exponential backoff (1s, 2s, 4s)
    - Timeout: 30 seconds using URLRequest timeoutInterval
    - Generic decoding with JSONDecoder
    - Error types: networkError, decodingError, timeout

    Use URLSession.shared.data(for:)
    Wrap in retry loop with Task.sleep for backoff
    Throw descriptive errors
  </action>
  <verify>
    - HTTPClient.get(url:) compiles
    - Uses async throws
    - Has retry loop with 3 attempts
    - Has 30 second timeout
  </verify>
  <done>HTTPClient ready for Quran API integration</done>
</task>

<task type="auto">
  <name>Task 2: Create CustomButton component</name>
  <files>Sources/Presentation/Components/CustomButton.swift</files>
  <action>
    CustomButton.swift per PROJECT_RULES.md Components pattern:
    - struct CustomButton: View
    - let title: String, let action: () -> Void
    - var isDense: Bool = false, var isFilled: Bool = true
    - var isLoading: Bool = false
    - enum ButtonVariant { case active, disabled }
    - Body: Button with conditional styling based on variants
    - Disabled when isLoading or variant == .disabled
    - Show spinner when isLoading (using ProgressView)

    Stateless except for loading prop
    Accept closure for action
    Reusable across features
  </action>
  <verify>
    - CustomButton compiles
    - Has isDense, isLoading, variant props
    - action closure works
    - Disabled state works
  </verify>
  <done>CustomButton ready for onboarding/auth UI</done>
</task>

<task type="auto">
  <name>Task 3: Add SwiftData relationships to User entity</name>
  <files>Sources/Domain/Entities/user.swift, Sources/Domain/Entities/session.swift, Sources/Domain/Entities/blocked_app.swift, Sources/Domain/Entities/app_time_limit.swift</files>
  <action>
    Create related entities (stub models for Phase 1):
    - session.swift: @Model class Session (date, duration, surahs)
    - blocked_app.swift: @Model class BlockedApp (bundleId, name, timeLimit)
    - app_time_limit.swift: @Model class AppTimeLimit (bundleId, dailyMinutes)

    Update user.swift:
    - Add @Relationship var sessions: [Session]?
    - Add @Relationship var blockedApps: [BlockedApp]?
    - Add @Relationship var appTimeLimits: [AppTimeLimit]?
    - Use deleteRule .cascade for relationships

    Follow SwiftData relationship patterns
    Use optional collections for new users
  </action>
  <verify>
    - User has @Relationship to Session
    - User has @Relationship to BlockedApp
    - User has @Relationship to AppTimeLimit
    - Related entities compile
    - xcodebuild succeeds
  </verify>
  <done>User entity has relationships for all features</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] xcodebuild succeeds
- [ ] HTTPClient has 3-retry with exponential backoff
- [ ] HTTPClient has 30s timeout
- [ ] CustomButton has loading/dense/disabled variants
- [ ] User has @Relationship to Session, BlockedApp, AppTimeLimit
</verification>

<success_criteria>
- HTTPClient handles network requests with retry
- CustomButton component reusable
- User entity complete with relationships
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
