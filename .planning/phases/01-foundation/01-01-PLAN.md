---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - surahfocus.xcodeproj/project.pbxproj
  - .gitignore
  - Sources/Core/SceneNavigation/Router.swift
  - Sources/RootView.swift
  - Sources/SurahFocusApp.swift
autonomous: true

must_haves:
  truths:
    - "Project builds without errors"
    - "Navigation stack works between screens"
    - "Router can navigate to routes and navigate back"
    - "Environment objects (router) injected at root"
  artifacts:
    - path: ".gitignore"
      provides: "Git ignore patterns for Xcode"
      contains: "xcuserdata"
    - path: "Sources/Core/SceneNavigation/Router.swift"
      provides: "Navigation path management"
      min_lines: 20
      exports: ["Router", "Route"]
    - path: "Sources/RootView.swift"
      provides: "Root navigation with environment injection"
      min_lines: 25
    - path: "Sources/SurahFocusApp.swift"
      provides: "App entry point"
      min_lines: 10
  key_links:
    - from: "SurahFocusApp.swift"
      to: "RootView"
      via: "WindowGroup root"
    - from: "RootView"
      to: "Router"
      via: "@StateObject injection"
    - from: "RootView"
      to: "NavigationStack"
      via: "path binding to router.navigationPath"
---

<objective>
Setup project infrastructure with git, folder structure, and navigation foundation.

Purpose: Create the foundation for all features - proper git ignore, Clean Architecture folder structure, working navigation.

Output: Building app with Router navigation, folder structure ready for DI/data layer.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/PROJECT_RULES.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STRUCTURE.md

Known issues to fix:
- iOS deployment target set to 26.0 (future) - needs fix to 17.0
- No .gitignore file
- Team ID hardcoded in project
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix project configuration issues</name>
  <files>surahfocus.xcodeproj/project.pbxproj, .gitignore</files>
  <action>
    1. Create .gitignore with standard iOS patterns (xcuserdata, *.hmap, DerivedData)
    2. In Xcode project: Change IPHONEOS_DEPLOYMENT_TARGET from 26.0 to 17.0
    3. In project.pbxproj: Replace hardcoded team ID with env var placeholder for local dev

    Use Edit tool to modify .pbxproj file - find IPHONEOS_DEPLOYMENT_TARGET lines and change 26.0 to 17.0
  </action>
  <verify>
    - grep IPHONEOS_DEPLOYMENT_TARGET surahfocus.xcodeproj/project.pbxproj shows 17.0
    - .gitignore contains xcuserdata
  </verify>
  <done>Deployment target fixed to iOS 17.0, .gitignore created, team ID configurable</done>
</task>

<task type="auto">
  <name>Task 2: Create Clean Architecture folder structure</name>
  <files>Sources/Core/, Sources/Data/, Sources/Domain/, Sources/Presentation/, Sources/Utils/</files>
  <action>
    Create folder structure per PROJECT_RULES.md:

    Sources/Core/DataDependency/DIContainer.swift (stub)
    Sources/Core/SceneNavigation/Router.swift
    Sources/Data/DataSource/LocalDataSource.swift (stub)
    Sources/Domain/Entities/ (placeholder .gitkeep)
    Sources/Domain/Services/ (placeholder .gitkeep)
    Sources/Presentation/Components/ (placeholder .gitkeep)
    Sources/Utils/Extensions.swift (stub)
    Sources/RootView.swift
    Sources/SurahFocusApp.swift (move existing)

    Delete default ContentView.swift, replace with RootView.swift
  </action>
  <verify>
    - ls Sources/Core/SceneNavigation/Router.swift exists
    - ls Sources/RootView.swift exists
    - ContentView.swift does not exist
  </verify>
  <done>Folders created per Clean Architecture, files organized</done>
</task>

<task type="auto">
  <name>Task 3: Implement Router with NavigationStack</name>
  <files>Sources/Core/SceneNavigation/Router.swift, Sources/RootView.swift, Sources/SurahFocusApp.swift</files>
  <action>
    Router.swift per PROJECT_RULES.md:
    - class Router: ObservableObject with @Published var navigationPath = NavigationPath()
    - enum Route: Hashable (add placeholder routes: .home, .quran)
    - func navigate(to route: Route), func navigateBack(), func replaceNavigationPath(with:)

    RootView.swift:
    - @StateObject private var router = Router()
    - NavigationStack(path: $router.navigationPath)
    - .environmentObject(router)
    - placeholder HomeView() as root destination

    SurahFocusApp.swift:
    - Change from ContentView to RootView

    Create placeholder HomeView.swift in Presentation/ for navigation testing
  </action>
  <verify>
    - xcodebuild -scheme surahfocus build succeeds
    - Router has navigate() and navigateBack() methods
    - RootView uses NavigationStack with router binding
  </verify>
  <done>Router navigation working, app builds and runs</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] xcodebuild succeeds with no errors
- [ ] .gitignore contains xcuserdata, DerivedData, *.hmap
- [ ] Deployment target is iOS 17.0
- [ ] Router.navigate(to:) compiles
- [ ] Router.navigateBack() compiles
- [ ] RootView injects router as environmentObject
</verification>

<success_criteria>
- Project builds without errors
- Navigation foundation in place (Router + NavigationStack)
- Folder structure matches Clean Architecture
- .gitignore prevents committing user-specific files
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
